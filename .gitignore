# Dependencies
node_modules/

# Environment — never commit API keys
.env
.env.local
.env.*.local

# Build output
dist/
build/

# OS
.DS_Store

# Editor
.vscode/
.idea/

# Wrangler local cache — contains account ID, never commit
.wrangler/
worker/.wrangler/

# Vite local dep cache
benebots/.vite/

# Wrangler local dev vars — test keys only, but never commit
worker/.dev.vars
.dev.vars
